# Baked into `npm run build` so Vercel does not need manual env for the default deploy.
# Override in Vercel → Environment Variables if you use a different API host.
VITE_API_BASE_URL=https://realtime-testing-dashboard-api.onrender.com
